/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['drive.google.com', 'your-other-domain.com'], // Add your image hosting domain here
    },
};

export default nextConfig;
