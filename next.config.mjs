/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['drive.google.com', 'https://drive.usercontent.google.com/'], // Add your image hosting domain here
    },
};

export default nextConfig;
